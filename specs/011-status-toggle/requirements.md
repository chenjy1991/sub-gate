# 011 - 状态切换功能

## 需求背景

节点管理和订阅管理页面的状态列当前仅静态显示"启用/禁用"Badge，无法直接在列表中切换状态，需要进入编辑页面才能修改。需要支持在列表页直接点击 Badge 切换启用/禁用状态。

## 功能列表

1. 节点管理列表页 — 状态列 Badge 可点击切换启用/禁用
2. 订阅管理列表页 — 状态列 Badge 可点击切换启用/禁用

## 交互设计

- 点击 Badge 直接切换状态（1 ↔ 0），无需确认弹窗（操作可逆）
- 切换期间显示 loading 态，防止重复点击
- 切换成功后刷新列表数据

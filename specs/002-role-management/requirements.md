# 角色管理 — 需求

## 背景
系统需要 RBAC 角色体系，管理员可以管理角色的增删改查。

## 功能需求
1. 3 个预设角色：管理员（ADMIN）、VIP用户（VIP）、注册用户（USER）
2. 角色列表（分页）、新增、编辑、删除
3. 前端弹窗表单操作，不跳转页面

## 数据字段
- 角色名称（name）
- 角色代码（code，唯一）
- 备注（remark）
- 状态（status，1=启用 0=禁用）
